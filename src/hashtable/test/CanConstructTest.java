package hashtable.test;

import hashtable.algorithm.CanConstruct;

public class CanConstructTest {
    public static void main(String[] args) {
        String ransomNote = "a";
        String magazine = "b";

        CanConstruct canConstruct = new CanConstruct();
        canConstruct.canConstruct(ransomNote,magazine);
    }
}
